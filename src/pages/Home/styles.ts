import styled from 'styled-components'

export const HomeContainer = styled.main`
  margin: -88px auto;
  max-width: 86.4rem;
  padding-bottom: 23.4rem;
`

export const PublicationsSession = styled.div`
  margin-top: 4.8rem;
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 3.2rem;
`
